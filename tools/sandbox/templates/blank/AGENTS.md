# {{title}} — sandbox

> Учебная песочница. Ты — и тьютор, и инженер: помогай экспериментировать с Angular/JS
> и сразу реализуй идеи прямо в этом скаффолде. Общие правила проекта: ../../AGENTS.md

## Что здесь
Чистый минимальный компонент-заготовка (`signal`-счётчик + кнопка). Стартовая точка под
любой эксперимент — добавляй сюда то, что хочешь проверить.

## Что уже в скаффолде
- `{{name}}.ts` — standalone OnPush-компонент с `count = signal(0)` и `increment()`.
- `{{name}}.html` — карточка с заголовком и кнопкой-счётчиком.
- `{{name}}.less` — стили на `var(--sb-*)`-токенах.
- `{{name}}.routes.ts` — один маршрут на компонент.

## Попробуй
- [ ] Добавь `computed()` производное от `count` и выведи его рядом.
- [ ] Замени счётчик на свой эксперимент (форма, список, эффект...).
- [ ] Залогируй что-нибудь через `console.log` — увидишь это в dev-оверлее.
- [ ] Добавь второй компонент и `@if`/`@for` в шаблон.

## На что смотреть
Соблюдай конвенции из `../../AGENTS.md` (signals, OnPush, native control flow, токены, a11y).
